# TODO


Backend
- [] Make Robustness KPI (avg Hours btwn tasks or amount of overtime, some metric on how robust is the plan to unforseen disruptions) Robustness to changes in weather.

Frontend 
- Change the configuration pane on the right hand side to be collapsable to the left with a little icons to click if we want to unfold it. Do the same for breakdowns, but rename it to Scenarios
- [] Style the gantt chart nicer with some drop shadows and not so bright colors. 
- [] Give tasks proper names instead of T1, T2 etc. 
- [] Make tasks dynamic. Double-clicking on the gantt chart should create a task which can be dragged on the timeline and shortened. Loading the page should give us the same default tasks as what is currently configured.
- [] When clicking on the optimized case, we want to see the delta in the revenue impact(e.g. +5%) 
- [] The timeline goes beyond what is currently needed with empty space. Let's clean that up